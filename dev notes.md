# Dev Notes

The file `README.md` is built by `build/build.py` using `build/README_src.md` and the images in the `imgs` folder

When running for the first time create the file `build/cache` (the file is used to prevent multiple same URL calls)